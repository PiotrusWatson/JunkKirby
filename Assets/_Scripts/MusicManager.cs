using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MusicManager : MonoBehaviour {

	/* TODO: Define logic for when enemies are on screen and when players are in fact on screen
	 * Maybe a) When enemy is within camera?
	 * b) When enemy spawns?
	 * c) When any enemy is in angery state? */
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
